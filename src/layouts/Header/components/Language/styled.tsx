import styled from "styled-components";

export const StyledSelectLen = styled.select`
    font-size: 14px;
    font-weight: 500;
    min-width: 90px;
    border-radius: 8px;
    color: #a14de2;
    cursor: pointer;
    border: none;
    outline: none;
`